import React from "react";
import "./style.css";

function Footer(props) {
    return(
        <div>This is the footer (for links, social, etc.)  Copyright info????</div>
    );
};

export default Footer;